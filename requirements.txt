# ABV-IIITM RAG chatbot — full dependency set.
# Fresh device:  python -m venv .venv
#                .venv\Scripts\activate      (Windows)   or   source .venv/bin/activate
#                pip install -r requirements.txt
# Fully local/free stack — no torch.

# stage 1 — crawl + download PDFs
requests>=2.31.0
beautifulsoup4>=4.12.0

# stage 2 — PDF -> Markdown (table-aware) + OCR for scanned PDFs
pymupdf4llm>=1.28.0
pymupdf>=1.26.0
rapidocr-onnxruntime>=1.2.0

# stage 3 — recursive chunking + metadata
langchain-text-splitters>=0.2.0

# stages 4-6 — embeddings, FAISS index, hybrid (BM25 + vector) search + rerank (ONNX, no torch)
fastembed>=0.3.0
faiss-cpu>=1.8.0
rank-bm25>=0.2.2
numpy>=1.26.0

# stages 7-8 — cited answers (LLM) + Streamlit UI + .env config
streamlit>=1.35.0
python-dotenv>=1.0.0

